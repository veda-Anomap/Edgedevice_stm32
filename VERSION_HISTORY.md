# Version History

<<<<<<< HEAD
이 문서는 프로젝트에서 실제로 적용된 변경을 시간 순서대로 정리한 기록입니다.
각 항목은 아래 형식으로 작성합니다.

- 변경 전 상태/문제
- 왜 바꿨는지(의사결정 배경)
- 무엇을 어떻게 바꿨는지(구현 방식)
- 변경 후 기능/안정성 개선 효과
=======
프로젝트 변경 이력을 시간 순서대로 기록합니다.
형식은 아래 4가지를 고정합니다.

- 변경 전 문제
- 변경 이유
- 구현 방식
- 변경 후 효과
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

---

## [v1.2.1] - 2026-03-20
<<<<<<< HEAD
### 외부/내부 워치독 최종 튜닝 + 큐 복구 반영

변경 전 상태/문제
- 워치독 로직은 동작했지만, IWDG 타임아웃(약 5초) 대비 모니터 miss 임계가 보수적으로 작아
  스케줄링 지터 상황에서 불필요한 리셋 가능성이 있었음.
- `.ioc` 재생성 과정에서 `control_queue` 길이가 `64 -> 16`으로 내려가 버려 burst 구간 내성이 약해질 수 있었음.
- 문서에 최신 워치독 파라미터(10초/3초/60초, miss 임계)가 명시되지 않았음.

왜 바꿨는지
- 외부 릴레이 복구 로직과 내부 IWDG 리셋 정책을 실제 운용 지터까지 고려해 안정적으로 맞추기 위함.
- RPi 명령 burst 상황에서 명령 유실/ACK fail 가능성을 낮추기 위함.

무엇을 어떻게 바꿨는지
- `SYS_MONITOR_MISS_MAX`를 `4`로 조정(1초 주기 기준, 4회 연속 미갱신 시 refresh 중단).
- `StartDefaultTask`의 grace/relay 대기 구간을 공통 helper 함수로 정리:
  - `rpi_wdg_wait_seconds_and_mark_alive()`
  - `rpi_wdg_sync_heartbeat_baseline()`
- `control_queue`를 코드/IOC 모두 `64`로 복구.
- README에 watchdog 정책과 큐 길이 정보를 최신 코드 기준으로 업데이트.

변경 후 기능/안정성 개선 효과
- 워치독 오탐 리셋 여유가 증가해 장시간 운용 안정성이 개선됨.
- 릴레이 컷/복구/재-grace 흐름의 코드 가독성과 유지보수성이 향상됨.
- burst 명령 수용성이 이전 수준으로 복구됨.
=======
### 외부/내부 워치독 튜닝 + 큐 복구

변경 전 문제
- IWDG 타임아웃(약 5초) 대비 모니터 miss 임계가 빡빡해 지터 상황에서 불필요 리셋 위험이 있었음.
- `.ioc` 재생성 중 `control_queue`가 `64 -> 16`으로 줄어 burst 내성이 약해짐.

변경 이유
- 외부 릴레이 워치독과 내부 IWDG의 실전 안정성을 높이기 위함.
- UART 명령 버스트 구간에서 드롭/ACK fail 가능성을 낮추기 위함.

구현 방식
- `SYS_MONITOR_MISS_MAX`를 `4`로 조정.
- `StartDefaultTask` 워치독 대기 구간을 helper 함수로 정리.
  - `rpi_wdg_wait_seconds_and_mark_alive()`
  - `rpi_wdg_sync_heartbeat_baseline()`
- `control_queue`를 코드/IOC 모두 `64`로 복구.
- README에 워치독 정책과 큐 길이 반영.

변경 후 효과
- 워치독 오탐 리셋 여유 증가.
- 릴레이 컷/복구/재-grace 흐름의 가독성 향상.
- burst 명령 수용성 복구.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/main.c`
- `irrled.ioc`
- `README.md`

---

## [v1.2.0] - 2026-03-19
<<<<<<< HEAD
### 한글 깨짐 정리 + 이력 문서 전면 재작성

변경 전 상태/문제
- 일부 소스 주석과 이력 문서에서 인코딩이 섞여 글자가 깨지는 구간이 남아 있었음.
- `VERSION_HISTORY.md`는 내용 자체가 읽기 어려워, 변경 목적과 효과를 추적하기 어려웠음.

왜 바꿨는지
- 유지보수 문서는 “누가 봐도 바로 이해되는 상태”가 기본이므로, 인코딩 깨짐을 남기면 디버깅/인수인계 속도가 크게 떨어짐.

무엇을 어떻게 바꿨는지
- `Core/Src/app.c`의 깨진 주석 블록을 정상 한글 문장으로 교체.
- `VERSION_HISTORY.md`를 UTF-8 기준으로 전면 재작성.
- 변경 이력을 단순 나열이 아니라, 문제-이유-구현-효과 구조로 재정리.

변경 후 기능/안정성 개선 효과
- 코드 리뷰와 장애 분석 시 문서 신뢰성이 복구됨.
- 팀원이 동일한 맥락으로 변경 의도를 재현할 수 있게 됨.
=======
### 한글 깨짐 복구 + 이력 문서 전면 재작성

변경 전 문제
- 일부 주석/문서에 인코딩 깨짐이 남아 추적이 어려웠음.

변경 이유
- 유지보수 시 변경 맥락을 정확히 남기기 위함.

구현 방식
- `Core/Src/app.c`의 깨진 주석 복구.
- `VERSION_HISTORY.md`를 UTF-8 기준으로 전면 재작성.

변경 후 효과
- 코드 리뷰/디버깅 시 문서 신뢰성 복구.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`
- `VERSION_HISTORY.md`

---

## [v1.1.4] - 2026-03-19
<<<<<<< HEAD
### 수동 모터 명령 버스트 완화 + 서보 안전 범위 축소

변경 전 상태/문제
- 수동 명령(`w/a/s/d`)이 짧은 시간에 몰리면 queue의 모든 명령이 연속 적용되어 서보가 과격하게 움직일 수 있었음.
- 기계적 종단 근처에서 급격한 왕복/충돌 느낌이 발생할 수 있었음.

왜 바꿨는지
- 실제 하드웨어에서 “획 도는 현상”과 극단 위치 떨림을 줄이기 위해, 제어를 부드럽게 제한할 필요가 있었음.

무엇을 어떻게 바꿨는지
- 수동 이동 명령을 coalescing 처리(최신 1개만 유지)하도록 `app.c`에 반영.
- `MANUAL_CMD_MIN_INTERVAL_MS`를 두어 최소 적용 간격을 강제.
- PAN/TILT PWM 한계를 기존보다 안쪽으로 조정(`motor_ctrl.h`)해 기계적 여유를 확보.

변경 후 기능/안정성 개선 효과
- 입력 버스트 시 서보가 한 번에 몰아 움직이는 현상이 완화됨.
- 종단 충돌 가능성 감소, 체감 안정성 증가.
=======
### 수동 명령 버스트 완화 + 서보 안전 범위 축소

변경 전 문제
- `w/a/s/d` 명령이 몰리면 서보가 급격히 연속 동작.
- 종단 근처 충돌/과동작 가능성.

변경 이유
- 실제 하드웨어에서 급격한 움직임과 끝단 충돌을 줄이기 위함.

구현 방식
- 이동 명령 coalescing(최신 1개 유지).
- `MANUAL_CMD_MIN_INTERVAL_MS`로 적용 간격 제한.
- PAN/TILT PWM 한계를 안쪽으로 축소.

변경 후 효과
- 급격한 왕복 감소.
- 종단 충돌 가능성 감소.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`
- `Core/Inc/motor_ctrl.h`

---

## [v1.1.3] - 2026-03-18
<<<<<<< HEAD
### UART1 프로토콜 파서 강건화(프레임 경계/타임아웃/큐 처리)

변경 전 상태/문제
- 임의 바이트가 CMD로 해석될 수 있어 프레임 경계가 흔들릴 위험이 있었음.
- 프레임 조립 중 입력이 끊기면 상태가 꼬여 invalid/oversize 누적 가능성이 있었음.
- 완료 프레임 버퍼가 단일 슬롯이면 버스트 입력에서 유실 위험이 큼.

왜 바꿨는지
- RPi 연동 시 간헐 실패를 줄이려면, 하드웨어 이상과 프로토콜 이상을 분리해서 처리해야 했음.

무엇을 어떻게 바꿨는지
- 허용 CMD를 `0x04/0x05`로 제한.
- `PROTO_RX_TIMEOUT_MS` 기반 프레임 조립 타임아웃 리셋 추가.
- 완료 프레임 큐를 다중 슬롯(`PROTO_FRAME_QUEUE_LEN`) 구조로 확장.
- `app_on_uart1_byte()`는 바이트 조립만 담당, 실제 프레임 처리/응답은 `ControlTask`에서 실행.
- 상태 응답(0x05)에 최소 응답 간격/펜딩 병합 로직 추가.

변경 후 기능/안정성 개선 효과
- 프로토콜 파싱 실패 시 복구가 빨라지고, 버스트 입력 내성이 개선됨.
- 수신과 처리 경로 분리로 태스크 지연 전파가 감소.
=======
### UART1 파서 강건화(프레임 경계/타임아웃/큐)

변경 전 문제
- 임의 바이트가 CMD로 해석될 가능성.
- 프레임 조립 중 입력 끊김 시 상태 꼬임.
- 단일 완료 버퍼 구조에서 버스트 유실 위험.

변경 이유
- RPi 연동 간헐 실패를 줄이고, 파싱 안정성을 높이기 위함.

구현 방식
- CMD allow-list(`0x04/0x05`) 적용.
- `PROTO_RX_TIMEOUT_MS` 기반 조립 타임아웃 리셋.
- 완료 프레임 큐 다중 슬롯화.
- 바이트 조립과 프레임 처리 경로 분리.

변경 후 효과
- 버스트 내성 향상.
- 파서 복구 속도 개선.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`
- `Core/Src/main.c`

---

## [v1.1.2] - 2026-03-18
<<<<<<< HEAD
### UART1 진단 카운터 확장 및 운영 분석 기준 추가

변경 전 상태/문제
- 통신 실패가 발생해도 원인이 하드웨어인지 프로토콜인지 즉시 구분하기 어려웠음.

왜 바꿨는지
- 현장 디버깅 시간을 줄이려면 에러 유형별 계측값이 필요했음.

무엇을 어떻게 바꿨는지
- UART1 진단 카운터를 세분화:
- 하드웨어 계층: `O/F/N/P`
- 프로토콜 계층: `IV/OV`
- 복구 계층: `R/RO/RF`
- 런타임 로그에 위 카운터를 포함해 관찰 가능하도록 정리.

변경 후 기능/안정성 개선 효과
- “라인 품질 문제”와 “프레임 포맷 문제”를 빠르게 분리 가능.
- 장애 재현 시 원인 축소 시간이 단축됨.

영향 파일
- `Core/Src/app.c`
- `VERSION_HISTORY.md`
=======
### UART1 진단 카운터 확장

변경 전 문제
- 통신 실패 원인(하드웨어/프로토콜) 분리가 어려움.

변경 이유
- 현장 디버깅 시간을 줄이기 위함.

구현 방식
- 하드웨어(`O/F/N/P`), 프로토콜(`IV/OV`), 복구(`R/RO/RF`) 카운터 분리.
- 로그 출력 포맷에 카운터 통합.

변경 후 효과
- 원인 분리 속도 향상.

영향 파일
- `Core/Src/app.c`
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

---

## [v1.1.1] - 2026-03-18
### UART1 에러 복구 루틴 추가

<<<<<<< HEAD
변경 전 상태/문제
- UART 오류 후 수신 인터럽트 재암이 실패하면 통신이 멈추는 경우가 있었음.

왜 바꿨는지
- 연속 운용에서 일시 오류가 전체 통신 중단으로 이어지지 않게 하려면 자동 복구가 필요했음.

무엇을 어떻게 바꿨는지
- `HAL_UART_ErrorCallback`에서 오류 비트 집계 후 복구 루틴 실행.
- 플래그 클리어 → Receive_IT 재암 → 실패 시 Abort 후 재시도 순으로 단계적 복구.

변경 후 기능/안정성 개선 효과
- 일시 노이즈/프레이밍 에러 이후에도 RX 경로가 복귀할 확률이 증가.
=======
변경 전 문제
- UART 오류 후 재암 실패 시 수신 중단 가능성.

변경 이유
- 일시 오류가 전체 통신 중단으로 이어지지 않게 하기 위함.

구현 방식
- 오류 플래그 클리어 -> `Receive_IT` 재암 -> 실패 시 abort 후 재시도.

변경 후 효과
- RX 복구 확률 증가.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`

---

## [v1.1.0] - 2026-03-18
<<<<<<< HEAD
### README/HISTORY 문서 체계 정리(코드 동작 기준 동기화)

변경 전 상태/문제
- 문서와 실제 코드 동작이 일부 불일치했고, 항목이 요약 중심이라 실무 추적성이 낮았음.

왜 바꿨는지
- 문서-코드 괴리를 줄여 신규 작업/디버깅 온보딩을 빠르게 하기 위함.

무엇을 어떻게 바꿨는지
- README를 현재 구조(I2S, RTOS, UART 프로토콜, 센서/모터 루프) 기준으로 재작성.
- HISTORY를 버전/배경/효과 중심 구조로 재정렬.

변경 후 기능/안정성 개선 효과
- 구현 의도와 운영 방법을 문서만 보고 파악 가능.
=======
### README/HISTORY 문서 구조 정리

변경 전 문제
- 문서와 실제 코드 흐름이 일부 불일치.

변경 이유
- 신규 작업/디버깅 온보딩을 빠르게 하기 위함.

구현 방식
- README를 현재 코드 구조 기준으로 재작성.
- HISTORY를 문제/이유/구현/효과 형식으로 재정렬.

변경 후 효과
- 문서만 읽어도 현재 구조 파악 가능.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `README.md`
- `VERSION_HISTORY.md`

---

## [v1.0.0] - 2026-03-18
<<<<<<< HEAD
### 마이크 경로 ADC → I2S 전환 및 신호 안정화 반영

변경 전 상태/문제
- ADC 기반 설계와 I2S 전환 코드가 혼재되어 기준점/스케일/노이즈 처리가 불명확했음.

왜 바꿨는지
- INMP441(I2S) 기반으로 방향 감지 성능을 유지하면서 기존 제어 로직과 호환해야 했음.

무엇을 어떻게 바꿨는지
- I2S 데이터 언팩(24-bit signed) 후 기존 처리 경로로 연결.
- 동적 DC 오프셋 추적(DC blocking), 노이즈 게이트 적용.
- short/long window 기반 신호/잡음 분리 및 SNR/비율 게이트 유지.
- ADC 경로는 조건부 컴파일 fallback로 유지.

변경 후 기능/안정성 개선 효과
- I2S 환경에서 기준점 흔들림과 저레벨 노이즈 오탐이 감소.
- 기존 모터 제어 로직을 크게 깨지 않고 이식 가능해짐.
=======
### 마이크 경로 ADC -> I2S 전환

변경 전 문제
- ADC/I2S 경로 혼재로 기준점/노이즈 처리 불명확.

변경 이유
- INMP441(I2S) 기반으로 방향 감지를 안정화하기 위함.

구현 방식
- 24-bit signed 데이터 언팩 후 기존 처리 경로와 연결.
- 동적 DC 오프셋 추적 + 노이즈 게이트 적용.
- short/long window + SNR/ratio 게이트 유지.

변경 후 효과
- 저레벨 노이즈 오탐 감소.
- 기존 제어 로직과의 호환 유지.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/mic.c`
- `Core/Inc/mic.h`
- `Core/Src/app.c`
- `Core/Src/main.c`
- `irrled.ioc`

---

## [v0.9.6] - 2026-03-10
<<<<<<< HEAD
### PCF8591 조도 센서 추가 + 상태 JSON 확장

변경 전 상태/문제
- 상태 응답(0x05)에 온습도/방향만 포함되어 주변 조도 정보를 전달하지 못했음.

왜 바꿨는지
- 상위 시스템에서 환경 데이터(조도)까지 함께 활용하려는 요구가 있었음.

무엇을 어떻게 바꿨는지
- I2C1 버스에 PCF8591(CH0) 읽기 모듈 추가.
- 상태 JSON에 `light` 필드 추가.

변경 후 기능/안정성 개선 효과
- RPi/서버 측에서 센서 상태를 단일 응답으로 수집 가능.
=======
### PCF8591 조도 센서 추가

변경 전 문제
- 상태 응답에 조도 정보가 없음.

변경 이유
- 상위 시스템에서 환경 데이터(조도) 활용 요구.

구현 방식
- I2C1 버스에 PCF8591(CH0) 추가.
- 상태 JSON에 `light` 필드 추가.

변경 후 효과
- 상태 응답 1회로 온습도/방향/조도 동시 수집 가능.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Inc/pcf8591.h` (신규)
- `Core/Src/pcf8591.c` (신규)
- `Core/Src/app.c`

---

## [v0.9.5] - 2026-03-05
<<<<<<< HEAD
### RTOS 경로 정리 + 실행 혼동 코드 제거

변경 전 상태/문제
- RTOS 시작 이후 도달하지 않는 `while(1)` 경로가 남아 있어 동작 해석에 혼동이 있었음.

왜 바꿨는지
- 실행 경로를 태스크 중심으로 명확히 고정하기 위함.

무엇을 어떻게 바꿨는지
- 태스크 기반 루프를 기준으로 메인 경로를 정리.
- 문서/설명에서도 RTOS 기준 흐름으로 통일.

변경 후 기능/안정성 개선 효과
- 코드 읽을 때 실제 실행 경로를 오해할 가능성이 줄어듦.
=======
### RTOS 실행 경로 정리

변경 전 문제
- RTOS 시작 후 도달하지 않는 코드가 남아 혼동 유발.

변경 이유
- 실행 경로를 태스크 중심으로 명확화하기 위함.

구현 방식
- 메인 루프/태스크 경로 기준으로 정리.

변경 후 효과
- 코드 해석 혼동 감소.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/main.c`

---

## [v0.9.4] - 2026-03-05
<<<<<<< HEAD
### ControlTask / SensorTask 역할 분리

변경 전 상태/문제
- 제어/센서/출력이 한 루프에 몰려 지연 전파 가능성이 컸음.

왜 바꿨는지
- 센서 지연(I2C 등)이 모터 제어 주기를 막지 않게 하려는 목적.

무엇을 어떻게 바꿨는지
- `app_control_loop()`와 `app_sensor_loop()` 분리.
- ControlTask(10ms) / SensorTask(20ms)로 주기 분배.

변경 후 기능/안정성 개선 효과
- 제어 응답성과 센서 처리의 간섭 감소.
=======
### ControlTask / SensorTask 분리

변경 전 문제
- 제어/센서/출력이 한 루프에 몰려 지연 전파.

변경 이유
- 센서 지연이 모터 제어 주기를 막지 않게 하기 위함.

구현 방식
- `app_control_loop()` / `app_sensor_loop()` 분리.
- Control 10ms, Sensor 20ms 주기 분배.

변경 후 효과
- 제어 응답성 개선.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Inc/app.h`
- `Core/Src/app.c`
- `Core/Src/main.c`

---

## [v0.9.3] - 2026-03-05
<<<<<<< HEAD
### UART ISR 최소화 + UartRxTask 활성화

변경 전 상태/문제
- ISR에서 파싱 비중이 커지면 인터럽트 점유 시간이 길어져 프레임 드롭 위험이 있었음.

왜 바꿨는지
- RTOS 구조에서는 ISR은 짧게, 파싱은 태스크에서 처리하는 것이 안전함.

무엇을 어떻게 바꿨는지
- ISR에서는 1바이트 큐 적재 + 즉시 리턴.
- `UartRxTask`가 큐에서 바이트를 받아 프레임 조립/파서 입력 담당.

변경 후 기능/안정성 개선 효과
- ISR 부하 감소, 전체 스케줄링 안정성 개선.
=======
### UART ISR 최소화 + RxTask 활성화

변경 전 문제
- ISR에서 파싱 비중이 커 인터럽트 점유 시간 증가.

변경 이유
- ISR은 짧게, 파싱은 태스크로 분리하기 위함.

구현 방식
- ISR에서는 1바이트 큐 적재만 수행.
- `UartRxTask`에서 바이트 수신/프레임 조립.

변경 후 효과
- ISR 부하 감소, 스케줄링 안정성 개선.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`
- `Core/Src/main.c`

---

## [v0.9.2] - 2026-03-04
<<<<<<< HEAD
### 모터 명령/모드 토큰 정리 및 충돌 방지

변경 전 상태/문제
- 단일 문자 모드 전환(`a/m`, `o/f`)과 이동 명령(`a/s/d/w`)이 충돌할 여지가 있었음.

왜 바꿨는지
- RPi JSON 명령(`auto/unauto`, `w/a/s/d`)과 로컬 테스트 명령을 명확히 분리하기 위함.

무엇을 어떻게 바꿨는지
- 프로토콜 토큰 allow-list를 명확화.
- 이동 명령 수신 시 필요 시 자동으로 MANUAL 전환 후 이동 적용.

변경 후 기능/안정성 개선 효과
- 명령 의미 충돌 감소, 모드 전환 동작 예측 가능성 향상.
=======
### 모터 명령 토큰 충돌 방지 정리

변경 전 문제
- 모드 문자와 이동 문자 의미 충돌 여지.

변경 이유
- RPi JSON 토큰(`auto/unauto`, `w/a/s/d`)과 로컬 입력 경계를 명확히 하기 위함.

구현 방식
- allow-list 및 모드 전환 처리 정리.

변경 후 효과
- 명령 해석 일관성 향상.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`

---

## [v0.9.1] - 2026-03-03
<<<<<<< HEAD
### AHT10 상태머신 + 이동평균(N=10) 도입

변경 전 상태/문제
- 센서 값을 너무 자주 읽거나 변환 대기 없이 읽으면 self-heating/오값 가능성이 있었음.

왜 바꿨는지
- 실제 운용에서 안정적인 온습도 값을 얻기 위해 측정 주기/필터/재시도 로직이 필요했음.

무엇을 어떻게 바꿨는지
- 측정 주기 최소 1초 강제, 기본 2초.
- 측정 명령 후 변환 대기(약 80ms), BUSY 비트 재확인.
- FIFO 기반 이동 평균(N=10) 적용.

변경 후 기능/안정성 개선 효과
- 스파이크 완화, 온습도 값의 시계열 안정성 향상.
=======
### AHT10 상태머신 + 이동평균(N=10)

변경 전 문제
- 과도한 측정/변환 대기 미준수 시 오측정 가능.

변경 이유
- self-heating/스파이크를 줄이고 안정값 확보.

구현 방식
- 최소 1초 측정주기 강제(기본 2초).
- 변환 대기(약 80ms), BUSY 재확인.
- FIFO 이동평균 적용.

변경 후 효과
- 온습도 시계열 안정성 향상.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/aht10.c`
- `Core/Inc/aht10.h`

---

## [v0.9.0] - 2026-03-02
<<<<<<< HEAD
### 프로젝트 분리 구조 정리(마이크/모터/앱 통합 제어)

변경 전 상태/문제
- 단일 파일에 기능이 몰려 구조 파악과 변경 영향 분석이 어려웠음.

왜 바꿨는지
- 기능별 책임을 분리해 유지보수성과 테스트 용이성을 높이기 위함.

무엇을 어떻게 바꿨는지
- `mic.c`, `motor_ctrl.c`, `app.c` 중심 구조로 역할 분리.
- `app.c`에서 상호 연계/모드 제어/프로토콜 처리 통합.

변경 후 기능/안정성 개선 효과
- 변경 범위 예측이 쉬워지고, 기능 추가 시 회귀 위험이 감소.
=======
### 기능 분리 구조 정리(마이크/모터/앱)

변경 전 문제
- 단일 파일 집중으로 변경 영향 분석이 어려움.

변경 이유
- 기능별 책임 분리로 유지보수성 확보.

구현 방식
- `mic.c`, `motor_ctrl.c`, `app.c` 중심 구조로 분리.

변경 후 효과
- 회귀 위험 감소, 변경 범위 예측 용이.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2

영향 파일
- `Core/Src/app.c`
- `Core/Src/mic.c`
- `Core/Src/motor_ctrl.c`
- `Core/Inc/*`

---

## 참고: Gemini/Codex 계획 반영 방향
<<<<<<< HEAD

다음 항목은 대화 중 반복 검토되어 최종 코드에 반영된 핵심 설계 원칙입니다.

- ISR 최소화: 인터럽트에서는 바이트 큐잉만 수행하고, 파싱/응답은 태스크에서 처리.
- 태스크 분리: 제어 루프와 센서 루프를 분리해 지연 전파 차단.
- 프로토콜 강건성: CMD 화이트리스트, LEN 검증, 프레임 타임아웃, 큐 기반 버스트 흡수.
- 진단 가능성: UART 오류를 유형별 카운터로 분리해 원인 분석 시간 단축.
- 모터 안전성: 명령 버스트 coalescing + 최소 적용 간격 + PWM 안전 범위 축소.
- 신호 안정성: I2S DC 블로킹/노이즈 게이트 + short/long window 기반 SNR 게이트 유지.
- 문서 동기화: 실제 코드 구조와 README/HISTORY를 지속 동기화.
=======

- ISR 최소화: 인터럽트에서는 큐잉만, 파싱/응답은 태스크에서 처리.
- 태스크 분리: 제어 루프/센서 루프를 분리해 지연 전파 차단.
- 프로토콜 강건성: CMD 검증, LEN 검증, 타임아웃, 버스트 흡수.
- 진단 강화: UART 오류 카운터 분리로 원인 분석 시간 단축.
- 모터 안정성: burst coalescing + 최소 적용 간격 + 안전 범위 적용.
- 신호 안정성: I2S DC 블로킹/노이즈 게이트 + 윈도우 기반 SNR 게이트 유지.
>>>>>>> b600a4a3fd0f5f85ce47a74061f634217e7f44d2
